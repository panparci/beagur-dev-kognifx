# Auth / Login override

Extends `design-system/MASTER.md`.

## Layout

- Split 50/50 desktop; stack mobile
- Left: brand panel — solid sidebar brown, logo + title + one line copy
- Right: ivory panel — form directly on background (no heavy card shadow)

## Style

- Polos — no feature cards, no decorative grid on auth panels
- Demo accounts: simple bordered list, badge role only
- CTA: `Button` primary copper

## Gradients

- Default: none on auth panels
- If needed: ivory→white card gradient only on right panel cards
