# Landing page override

Extends `design-system/MASTER.md`.

## Scope

- Hero & stats: minimal changes unless requested
- Map section `#sebaran-guru`: large, full Indonesia bounds, overlay gradient ivory→transparent only

## Map

- `IndonesiaTeachersMap` + `PublicTeachersProvider`
- Markers: `bea-copper` / `bea-copper-dark`
- No zoom interaction (`lockZoom` + `fitBounds`)
